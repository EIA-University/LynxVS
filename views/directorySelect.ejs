<!DOCTYPE html>
<html lang="en">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1.0"/>
    <title><%=title%></title>

    <!-- CSS  -->
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link href="/materialize/css/materialize.min.css" type="text/css" rel="stylesheet" media="screen,projection"/>
    <!--<link href="/css/style.css" type="text/css" rel="stylesheet" media="screen,projection"/>-->
        <link href="/css/devicon.css" type="text/css" rel="stylesheet" />
    <link href="/css/bootstrap.css" type="text/css" rel="stylesheet" />
</head>

<body>
<main>
    <div class="container">
        <div class="section">
            <% for (var i = 0; i < folders.length; i++) { %>
                <% if (i % 6 == 0) { %>
                    <% if (i == 0) {%>
            <div class="row">
                    <% } else { %>
            </div>
                        <% if (i != folders.length-1) { %>
            <div class="row">
                        <% } %>
                    <% } %>
                <% } %>
                <div class="col-sm-2 col-md-2 folderClick">
                    <div class="thumbnail">
                        <img src="/icons/custom/folderColor.png" alt="...">
                        <div class="caption center">
                            <p><%= folders[i]%></p>
                        </div>
                    </div>
                </div>
            <% } %>
            </div>
        </div>
    </div>

    <!-- Le Fab -->
    <div class="fixed-action-btn">
        <a id="fab" class="btn-floating btn-large red">
            <i class="large material-icons">keyboard_return</i>
        </a>
        <ul>
            <li><a href="#modal1" class="btn-floating orange tooltipped" data-position="left" data-delay="50" data-tooltip="Create Folder"  data-toggle="modal" data-target="#myModal"><i class="material-icons">create_new_folder</i></a></li>
        </ul>
    </div>

    <!-- Modal -->
    <div id="myModal" class="modal fade" role="dialog">
        <div class="modal-dialog">

            <!-- Modal content-->
            <div class="modal-content">
                <form id="addFolder" method="post" action="/pepe">
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal">&times;</button>
                        <h4 class="modal-title">New Folder</h4>
                    </div>
                    <div class="modal-body">
                        <div class="form-group">
                            <input type="text" class="form-control" id="folderName" name="folderName" placeholder="Folder Name" required>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button id="sub" type="submit" class="btn btn-default" data-dismiss="modal">Add Folder</button>
                        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

</main>


<!--  Scripts-->
<script src="/js/jquery.min.js"></script>
<script src="/materialize/js/materialize.min.js"></script>
<script src="/js/bootstrap.js"></script>
<script src="/js/selectDir.js"></script>


</body>
</html>
